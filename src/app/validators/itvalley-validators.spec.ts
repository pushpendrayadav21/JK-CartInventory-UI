import { ITValleyValidators } from './itvalley-validators';

describe('ITValleyValidators', () => {
  it('should create an instance', () => {
    expect(new ITValleyValidators()).toBeTruthy();
  });
});
