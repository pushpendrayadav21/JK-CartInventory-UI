import { ProductCategoryModel } from './product-category-model';

describe('ProductCategoryModel', () => {
  it('should create an instance', () => {
    expect(new ProductCategoryModel()).toBeTruthy();
  });
});
